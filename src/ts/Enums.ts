export enum Cameras {
	Front = "front",
	LeftRepeater = "left_repeater",
	RightRepeater = "right_repeater",
	Back = "back"
}

export enum VideoEntryTypes {
	Unknown = null,
	Sentry = "SentryClips",
	Saved = "SavedClips",
	Recent = "RecentClips"
}